Javascript
javascrip are functions that can improve your website.
nodeJS is an application to run javascript.
EXAMPLE:

const a = 100;
const b = (a,b)=>{
  return 10
  };
console.log(b);
const c = b;
console.log(c);
const d = b(5,8);
console.log(d);
const e= b(1,5);
console.log(e+d);



Javascript commands:

node song.js <- to run "name of the java file"
const: a box where you can put a variable, a fuction
functions: a set of instructions 
add strings: characters
= : whats on the right will be the value of whats on the left
== : comparing
=== : exactly equals to
console.log(whatever you want to print) :  to print
return: 


Example:
const a = 'song'
const b = () => {
  console.log(a);
  return a + 'hi';
  };

  if (b()==='song'){
    console.log('hi');
    }

